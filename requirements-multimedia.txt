# Multimedia Manipulation Packages for Digital Artifact Hacking
# Audio, Video, Photo, Signal Processing, Glitch Effects

# ========================================
# AUDIO MANIPULATION
# ========================================

# Core audio libraries
librosa>=0.10.0              # Music and audio analysis
soundfile>=0.12.0            # Read/write audio files
pydub>=0.25.1                # Simple audio manipulation
audioread>=3.0.0             # Audio file reading (multiple backends)
scipy>=1.11.0                # Signal processing (scipy.signal)
numpy>=1.24.0                # Numerical operations (required by most)

# Advanced audio processing
pyrubberband>=0.3.0          # Time-stretching and pitch-shifting
pyaudio>=0.2.13              # Real-time audio I/O (recording/playback)
madmom>=0.16.1               # Music information retrieval (beat tracking, etc.)
essentia>=2.1b6              # Audio analysis (Spotify uses this!)
aubio>=0.4.9                 # Pitch detection, onset detection
pysoundfile>=0.10.0          # Audio file I/O

# Synthesis and generation
pedalboard>=0.7.0            # Spotify's audio effects library!
scipy>=1.11.0                # Signal generation (already listed)

# MIDI and music theory
mido>=1.3.0                  # MIDI file reading/writing
music21>=9.1.0               # Music theory, notation, analysis
pretty_midi>=0.2.10          # MIDI manipulation

# ========================================
# VIDEO MANIPULATION
# ========================================

# Core video libraries
moviepy>=1.0.3               # Video editing, compositing
opencv-python>=4.8.0         # Computer vision, frame manipulation
ffmpeg-python>=0.2.0         # FFmpeg wrapper for Python
imageio>=2.31.0              # Video I/O (used by moviepy)
imageio-ffmpeg>=0.4.9        # FFmpeg backend for imageio

# Advanced video processing
scikit-video>=1.1.11         # Video processing algorithms
av>=10.0.0                   # PyAV (libav/ffmpeg bindings)

# ========================================
# PHOTO/IMAGE MANIPULATION
# ========================================

# Core image libraries
Pillow>=10.0.0               # PIL fork, image processing
scikit-image>=0.21.0         # Image processing algorithms
opencv-python>=4.8.0         # (already listed for video)
imageio>=2.31.0              # (already listed)

# Advanced image processing
matplotlib>=3.7.0            # Plotting, image display
seaborn>=0.12.0              # Statistical visualizations
scikit-learn>=1.3.0          # Machine learning (for image classification, etc.)

# Image manipulation effects
wand>=0.6.11                 # ImageMagick bindings (advanced effects)

# ========================================
# GLITCH & ARTIFACT EFFECTS
# ========================================

# Noise and random generation
numpy>=1.24.0                # (already listed)
scipy>=1.11.0                # (already listed)
noise>=1.2.2                 # Perlin noise, simplex noise

# Data corruption techniques
bitstring>=4.1.0             # Binary data manipulation
struct                       # (built-in) - binary packing/unpacking

# ========================================
# SIGNAL PROCESSING (DSP)
# ========================================

# Core DSP
scipy>=1.11.0                # (already listed) - scipy.signal, scipy.fft
numpy>=1.24.0                # (already listed)

# Specialized DSP
pyroomacoustics>=0.7.0       # Room acoustics simulation, reverb
pysndfx>=0.3.6               # SoX audio effects wrapper
soxr>=0.3.7                  # High-quality sample rate conversion

# Spectral processing
librosa>=0.10.0              # (already listed) - spectral features
matplotlib>=3.7.0            # (already listed) - spectrograms

# ========================================
# MACHINE LEARNING / AI (for effects)
# ========================================

# ML libraries
scikit-learn>=1.3.0          # (already listed)
tensorflow>=2.13.0           # Deep learning (optional, heavy)
torch>=2.0.0                 # PyTorch (alternative to TF)
torchaudio>=2.0.0            # Audio for PyTorch

# Pre-trained models
transformers>=4.30.0         # HuggingFace models (audio/image)

# ========================================
# UTILITIES
# ========================================

# File handling
pathlib                      # (built-in)
glob                         # (built-in)
os                           # (built-in)
shutil                       # (built-in)

# Data structures
pandas>=2.0.0                # Data manipulation
h5py>=3.9.0                  # HDF5 file format (large datasets)

# Parallel processing
joblib>=1.3.0                # Parallel execution
multiprocessing              # (built-in)

# Progress bars
tqdm>=4.65.0                 # Progress bars for loops

# Configuration
pyyaml>=6.0                  # YAML config files
python-dotenv>=1.0.0         # .env file loading

# ========================================
# WEB SCRAPING (for research skills)
# ========================================

beautifulsoup4>=4.12.0       # HTML parsing
requests>=2.31.0             # HTTP requests
lxml>=4.9.0                  # XML/HTML parsing (faster than html.parser)
selenium>=4.11.0             # Browser automation (for JS-heavy sites)

# ========================================
# NLPTEXT PROCESSING (for research)
# ========================================

nltk>=3.8.0                  # Natural language toolkit
spacy>=3.6.0                 # Industrial NLP
scikit-learn>=1.3.0          # (already listed) - TF-IDF

# ========================================
# VISUALIZATION
# ========================================

matplotlib>=3.7.0            # (already listed)
seaborn>=0.12.0              # (already listed)
plotly>=5.15.0               # Interactive plots
bokeh>=3.2.0                 # Interactive visualizations

# ========================================
# TESTING & QUALITY
# ========================================

pytest>=7.4.0                # Testing framework
black>=23.7.0                # Code formatter
flake8>=6.1.0                # Linting
mypy>=1.5.0                  # Type checking

# ========================================
# OPTIONAL: Heavy ML models (install separately if needed)
# ========================================

# tensorflow>=2.13.0         # (listed above)
# torch>=2.0.0               # (listed above)
# torchaudio>=2.0.0          # (listed above)

# Uncomment these if you want full ML capability:
# transformers>=4.30.0       # (listed above)
# diffusers>=0.21.0          # Stable Diffusion & image generation
# accelerate>=0.21.0         # HuggingFace optimization
